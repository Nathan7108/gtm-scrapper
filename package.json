{
  "name": "gtm-scrapper",
  "version": "1.0.0",
  "description": "GTM Intel — Personalized GTM Intelligence Feed",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "node server/index.js",
    "client": "cd client && npm run dev",
    "install:all": "npm install && cd client && npm install"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.100.1",
    "@the-convocation/twitter-scraper": "^0.22.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "node-cron": "^4.2.1",
    "resend": "^6.9.4"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
